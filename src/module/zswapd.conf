# shellcheck disable=SC2148

# Load the zram kernel module at boot time to enable compressed RAM-based swap
# devices, which can improve system performance and responsiveness, especially
# on systems with limited physical memory. This ensures that zram is available
# early in the boot process for swap configuration.
zram
